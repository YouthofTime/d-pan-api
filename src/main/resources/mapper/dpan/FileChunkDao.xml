<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zym.dpan.dao.FileChunkDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zym.dpan.entity.FileChunkEntity" id="fileChunkMap">
        <result property="id" column="id"/>
        <result property="identifier" column="identifier"/>
        <result property="realPath" column="real_path"/>
        <result property="chunkNumber" column="chunk_number"/>
        <result property="expirationTime" column="expiration_time"/>
        <result property="createUser" column="create_user"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, identifier, real_path, chunk_number, expiration_time, create_user, create_time
    </sql>

    <select id="selectAllByIdentifier" resultMap="fileChunkMap">
        select
            <include refid="Base_Column_List"/>
        from
            d_pan_file_chunk
        where
            identifier= #{identifier}
        and
            create_user = #{userId}

    </select>

    <insert id="insert" parameterType="com.zym.dpan.entity.FileChunkEntity">
        insert into d_pan_file_chunk (id, identifier, real_path,
                                      chunk_number, expiration_time, create_user,
                                      create_time)
        values (#{id}, #{identifier}, #{realPath},
                #{chunkNumber}, #{expirationTime}, #{createUser},
                #{createTime})
    </insert>

    <select id="selectUploadedChunkCount" resultType="integer">
        select
            COUNT(DISTINCT chunk_number)
        from
            d_pan_file_chunk
        where identifier= #{identifier} AND creat_user=#{userId}
    </select>

    <delete id="deleteByIdentifierAndUserId">
        DELETE FROM
            d_pan_file_chunk
        WHERE
            identifier = #{identifier}
        AND create_user = #{userId}
    </delete>
</mapper>